Project organization
====================
HAS_STANDALONE_PREDECESSOR
simple vs complex project structure
structure of the cmakelist.txt file
no project can depend on anything other than math and library, except game (
  engine is required to support the same dlls for now, until we have a build
  system).
